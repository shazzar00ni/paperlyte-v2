name: Analysis
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
permissions:
  contents: read
  pull-requests: read
jobs:
  sonar:
    name: SonarCloud
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
      - name: Scan
        # Pinned SHA for sonarcloud-github-action v3.1.0
        # gitleaks:allow
        uses: SonarSource/sonarcloud-github-action@e2d5840d02958999370866eb3033a04293921f04
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
