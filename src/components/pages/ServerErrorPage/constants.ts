/**
 * Button labels for ServerErrorPage component
 * Exported separately to maintain React Fast Refresh compatibility
 */
export const BUTTON_LABELS = {
  RETRY: 'Retry loading the page',
  HOMEPAGE: 'Return to homepage',
} as const;
