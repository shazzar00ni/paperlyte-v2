# Public Assets

This directory contains static assets for the Paperlyte landing page.

## Generated Files

The following files are automatically generated during the build process and are **not tracked in Git**:

### Icons and Favicons

Generated by `npm run generate:icons` (runs during `prebuild`):

- `favicon-16x16.png`
- `favicon-32x32.png`
- `favicon.ico`
- `apple-touch-icon.png`
- `android-chrome-192x192.png`
- `android-chrome-192x192.webp`
- `android-chrome-192x192.avif`
- `android-chrome-512x512.png`
- `android-chrome-512x512.webp`
- `android-chrome-512x512.avif`

**Source:** `public/favicon.svg`
**Script:** `scripts/generate-icons.js`
**Documentation:** See `ICON-GENERATION.md` for details

### Mockup Images

Generated by `npm run generate:mockups` (runs during `prebuild`):

- `mockups/notes-list.png`
- `mockups/notes-list.webp`
- `mockups/notes-list.avif`
- `mockups/note-detail.png`
- `mockups/note-detail.webp`
- `mockups/note-detail.avif`

**Source:** `public/mockups/*.svg` files
**Script:** `scripts/generate-mockups.js`
**Documentation:** See `SOCIAL-IMAGE-GENERATION.md` for details

### Sitemap

Generated by `npm run postbuild` (runs after build):

- `sitemap.xml`

**Script:** `scripts/generate-sitemap.cjs`
**Details:** The sitemap includes `<lastmod>` dates that reflect the last Git commit date for each source file (index.html, privacy.html, terms.html), ensuring accurate freshness signals for search engines.

## Source Files

These files **are** tracked in Git:

- `favicon.svg` - Source for all icons/favicons
- `index.md` - AI-facing Markdown representation of the landing page (see below)
- `mockups/*.svg` - Source files for mockup images
- `privacy.html` - Privacy policy page
- `terms.html` - Terms of service page
- Legal documents and other static content

## AI-Facing Markdown Representation (`index.md`)

`public/index.md` is a pre-authored, static Markdown mirror of the Paperlyte landing page. It is served by the `markdown-response` Netlify edge function (`netlify/edge-functions/markdown-response.ts`) when a client sends `Accept: text/markdown` on a `GET /` request.

**Why it exists:** The landing page is a client-side React SPA. The HTML shell (`index.html`) contains only a skip link and an empty `<div id="root">` — all content is mounted after JavaScript runs. Clients that parse the raw HTML (AI agents, LLMs, curl scripts) receive nearly empty output without this file.

**When to update `index.md`:** Any time the following source constants change, `index.md` must be updated to stay in sync:

| Section in `index.md` | Source file |
|---|---|
| Features | `src/constants/features.ts` |
| Mobile | `src/components/sections/Mobile/Mobile.tsx` |
| Statistics | `src/components/sections/Statistics/Statistics.tsx` |
| Comparison table | `src/constants/comparison.ts` |
| Testimonials | `src/constants/testimonials.ts` |
| Join the Waitlist (EmailCapture) | `src/components/sections/EmailCapture/emailCapture.data.ts` |
| FAQ | `src/constants/faq.ts` |
| Launch quarter / waitlist count | `src/constants/waitlist.ts` |

**Content update checklist:**

1. Edit `index.md` to match any copy or data changes in the source constants above.
2. Keep the comment block at the top of `index.md` intact — it records this maintenance requirement for future contributors.
3. The edge function serves the file as-is; no build step is required after editing.

## Build Process

1. **prebuild**: Generates icons and mockups from SVG sources
2. **build**: Compiles TypeScript and bundles the application
3. **postbuild**: Generates sitemap.xml with current lastmod dates

All generated files will be created automatically when you run:

```bash
npm run build
```

## CI/CD

Generated files are created during the CI/CD build process, ensuring they're always up-to-date in production deployments without cluttering the Git repository.
