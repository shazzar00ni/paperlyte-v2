# eslint-plugin-react-hooks@7.x declares peer dep support for ESLint ≤9,
# but eslint was bumped to 10.x by dependabot before the plugin updated its
# peer dep range. The plugin is compatible at runtime; this flag suppresses
# the false-positive ERESOLVE error until react-hooks releases an update.
legacy-peer-deps=true
