name: "CodeQL Security Scan"

# IMPORTANT: This is an advanced CodeQL configuration.
# To use this workflow, you MUST disable the default CodeQL setup in your repository settings:
# Go to Settings > Code security and analysis > Code scanning > CodeQL analysis
# and disable the default setup. Otherwise, you'll get the error:
# "CodeQL analyses from advanced configurations cannot be processed when the default setup is enabled"

on:
  push:
    branches: [main, develop]
    paths:
      - 'src/**'
      - 'public/**'
      - '.github/workflows/codeql.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'src/**'
      - 'public/**'
      - '.github/workflows/codeql.yml'
  schedule:
    # Run weekly on Monday at 9:00 AM UTC
    - cron: '0 9 * * 1'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    name: Analyze Code
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: ['javascript']

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          queries: security-extended,security-and-quality

      - name: Autobuild
        uses: github/codeql-action/autobuild@v3

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:${{matrix.language}}"
