# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

# This workflow requires that you have an existing account with codescan.io
# For more information about configuring your workflow,
# read our documentation at https://github.com/codescan-io/codescan-scanner-action
name: CodeScan

on:
  push:
    branches: [ "main" ]
  pull_request:
    # The branches below must be a subset of the branches above
    branches: [ "main" ]
  schedule:
    - cron: '41 21 * * 5'

permissions:
  contents: read

jobs:
    CodeScan:
        permissions:
          contents: read # for actions/checkout to fetch code
          security-events: write # for github/codeql-action/upload-sarif to upload SARIF results
          actions: read # only required for a private repository by github/codeql-action/upload-sarif to get the Action run status
        runs-on: ubuntu-latest
        steps:
            -   name: Checkout repository
                uses: actions/checkout@v6
            -   name: Cache files
                uses: actions/cache@v5
                with:
                    path: |
                        ~/.sonar
                    key: ${{ runner.os }}-sonar
                    restore-keys: ${{ runner.os }}-sonar
            -   name: Run Analysis
                id: codescan-analysis
                uses: codescan-io/codescan-scanner-action@955d3d1b4a9e374064189719484005ce8e08a749
                # Continue even if CodeScan fails (e.g., missing secrets in forks)
                continue-on-error: true
                with:
                    login: ${{ secrets.CODESCAN_AUTH_TOKEN }}
                    organization: ${{ secrets.CODESCAN_ORGANIZATION_KEY }}
                    projectKey: ${{ secrets.CODESCAN_PROJECT_KEY }}
            # Upload the SARIF file generated in the previous step
            # NOTE: Temporarily allowing failures due to GitHub's July 2025 breaking change
            # that rejects SARIF files with multiple runs per category. CodeScan may generate
            # multiple runs which is now rejected by the updated CodeQL action.
            # See: https://github.blog/changelog/2025-07-21-code-scanning-will-stop-combining-multiple-sarif-runs-uploaded-in-the-same-sarif-file/
            -   name: Upload SARIF file
                uses: github/codeql-action/upload-sarif@b20883b0cd1f46c72ae0ba6d1090936928f9fa30 # v4.32.0
                continue-on-error: true
                if: always() && hashFiles('codescan.sarif') != ''
                with:
                    sarif_file: codescan.sarif
                    category: codescan
            -   name: CodeScan Summary
                if: always()
                run: |
                    echo "## 🔬 CodeScan Analysis" >> $GITHUB_STEP_SUMMARY
                    echo "" >> $GITHUB_STEP_SUMMARY
                    
                    if [ "${{ steps.codescan-analysis.outcome }}" == "success" ]; then
                        echo "✅ **CodeScan:** Analysis completed successfully" >> $GITHUB_STEP_SUMMARY
                    elif [ "${{ steps.codescan-analysis.outcome }}" == "failure" ]; then
                        echo "⚠️ **CodeScan:** Analysis failed (check authentication/configuration)" >> $GITHUB_STEP_SUMMARY
                        echo "" >> $GITHUB_STEP_SUMMARY
                        echo "Ensure the following secrets are configured:" >> $GITHUB_STEP_SUMMARY
                        echo "- \`CODESCAN_AUTH_TOKEN\`" >> $GITHUB_STEP_SUMMARY
                        echo "- \`CODESCAN_ORGANIZATION_KEY\`" >> $GITHUB_STEP_SUMMARY
                        echo "- \`CODESCAN_PROJECT_KEY\`" >> $GITHUB_STEP_SUMMARY
                    else
                        echo "⏭️ **CodeScan:** Analysis skipped (missing configuration)" >> $GITHUB_STEP_SUMMARY
                    fi
                    
                    if [ -f "codescan.sarif" ]; then
                        echo "" >> $GITHUB_STEP_SUMMARY
                        echo "📄 SARIF results uploaded to GitHub Code Scanning." >> $GITHUB_STEP_SUMMARY
                    fi
                    
                    echo "" >> $GITHUB_STEP_SUMMARY
                    echo "📊 View detailed findings in the **Security** tab." >> $GITHUB_STEP_SUMMARY
