# Disable overly strict documentation linters
# Terminology linter flags 100+ minor docs wording issues (url vs URL, E2E vs end-to-end, etc.)
VALIDATE_NATURAL_LANGUAGE=false

# Spell check linter causes false positives in technical docs
VALIDATE_SPELL_CODESPELL=false
