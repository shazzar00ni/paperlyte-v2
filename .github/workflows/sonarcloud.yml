name: SonarCloud analysis

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  Analysis:
    runs-on: ubuntu-latest
    if: github.event.pull_request.head.repo.full_name == github.repository || github.event_name == 'push'

    steps:
      - name: Checkout
        uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332
        with:
          fetch-depth: 0

      - name: Analyze with SonarCloud
        uses: SonarSource/sonarcloud-github-action@ffc3010689be73b8e5ae0c57ce35968afd7909e8
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=${{ vars.SONAR_PROJECT_KEY }}
            -Dsonar.organization=${{ vars.SONAR_ORGANIZATION }}
            -Dsonar.sources=src
            -Dsonar.tests=src,scripts/__tests__
            -Dsonar.test.inclusions=src/**/*.test.ts,src/**/*.test.tsx,src/**/test/**,scripts/__tests__/**
            -Dsonar.exclusions=src/**/*.test.ts,src/**/*.test.tsx,src/**/test/**,scripts/maintenance/**,node_modules/**
            -Dsonar.coverage.exclusions=src/**/*.test.ts,src/**/*.test.tsx,src/**/test/**,scripts/**,node_modules/**
          projectBaseDir: .
