# Netlify Configuration
# Documentation: https://docs.netlify.com/configure-builds/file-based-configuration/

[build]
  # Base directory - explicitly set to repo root to override any UI settings
  # This prevents "invalid root directory 'backend'" errors
  base = "."

  # Build command
  command = "npm run build"

  # Directory with the build output (relative to repo root)
  publish = "dist"

  # Serverless functions directory
  functions = "netlify/functions"

  # Edge functions directory
  edge_functions = "netlify/edge-functions"

  # Node version to use
  environment = { NODE_VERSION = "20" }

# Production context: all deploys from the main branch
[context.production]
  command = "npm run build"
  publish = "dist"

# Deploy Preview context: all Deploy Previews from pull requests
[context.deploy-preview]
  command = "npm run build"
  publish = "dist"

# Branch Deploy context: all deploys from branches that aren't main
[context.branch-deploy]
  command = "npm run build"
  publish = "dist"

# WAF edge function — runs on every request before the origin
[[edge_functions]]
  function = "waf"
  path = "/*"

# Redirect rules for SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# ---------------------------------------------------------------------------
# Security headers — applied to all responses
# ---------------------------------------------------------------------------
[[headers]]
  for = "/*"
  [headers.values]
    # Prevent clickjacking
    X-Frame-Options = "DENY"

    # Prevent MIME type sniffing
    X-Content-Type-Options = "nosniff"

    # Referrer policy
    Referrer-Policy = "strict-origin-when-cross-origin"

    # Strict Transport Security (2 years, include subdomains, preload-eligible)
    Strict-Transport-Security = "max-age=63072000; includeSubDomains; preload"

    # Disable access to sensitive browser APIs
    Permissions-Policy = "geolocation=(), microphone=(), camera=(), payment=(), usb=(), bluetooth=(), serial=(), magnetometer=(), gyroscope=(), accelerometer=(), ambient-light-sensor=()"

    # Content Security Policy — all assets self-hosted; connects only to self
    Content-Security-Policy = "default-src 'self'; script-src 'self'; style-src 'self'; font-src 'self'; img-src 'self' data:; connect-src 'self'; worker-src 'none'; frame-src 'none'; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'none'; upgrade-insecure-requests;"

    # Prevent this site from being embedded by other origins (cross-origin)
    Cross-Origin-Opener-Policy = "same-origin"

    # Prevent other origins from loading this site's resources
    Cross-Origin-Resource-Policy = "same-origin"

    # Prevent cross-origin data leaks in embedded iframes (requires CORP above)
    Cross-Origin-Embedder-Policy = "require-corp"

    # Block Adobe/Silverlight cross-domain policy files
    X-Permitted-Cross-Domain-Policies = "none"

    # Legacy XSS auditor (defence-in-depth for older browsers)
    X-XSS-Protection = "1; mode=block"

# Deploy preview & branch deploys: prevent search-engine indexing
[[headers]]
  for = "/*"

  [headers.values]
    # Overridden per-context below for production; default suppresses indexing
    # on deploy previews (Netlify applies production headers only on production)

# Cache static assets for 1 year (content-addressed filenames ensure freshness)
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Cache self-hosted fonts for 1 year
[[headers]]
  for = "/fonts/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.woff2"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Prevent indexing of deploy previews and branch deploys
[[context.deploy-preview.headers]]
  for = "/*"
  [context.deploy-preview.headers.values]
    X-Robots-Tag = "noindex, nofollow, noarchive, nosnippet"

[[context.branch-deploy.headers]]
  for = "/*"
  [context.branch-deploy.headers.values]
    X-Robots-Tag = "noindex, nofollow, noarchive, nosnippet"

# Performance optimizations
# Skip Netlify's build processing since Vite handles bundling and optimization
[build.processing]
  skip_processing = true
