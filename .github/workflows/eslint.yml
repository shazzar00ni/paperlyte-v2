# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
# ESLint is a tool for identifying and reporting on patterns
# found in ECMAScript/JavaScript code.
# More details at https://github.com/eslint/eslint
# and https://eslint.org

name: ESLint

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '21 15 * * 2'

# Using 'pull_request' (not 'pull_request_target') means this workflow runs in
# a restricted context where untrusted fork code has no access to secrets.
# Adding security-events:write here is safe: it only allows writing to the
# Security tab and does not elevate code execution privileges.
permissions:
  contents: read
  security-events: write

jobs:
  eslint:
    name: Run eslint scanning
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Install SARIF formatter
        run: npm install --no-save @microsoft/eslint-formatter-sarif@3.1.0

      - name: Run ESLint
        id: eslint-run
        env:
          SARIF_ESLINT_IGNORE_SUPPRESSED: "true"
        run: |
          npx eslint "src/**/*.{js,jsx,ts,tsx}" \
            --format @microsoft/eslint-formatter-sarif \
            --output-file eslint-results.sarif \
            --max-warnings 0
        continue-on-error: true

      - name: Ensure SARIF file exists
        if: always()
        # Creates a minimal valid SARIF file if ESLint didn't produce one.
        # This can happen if ESLint fails before generating output.
        run: |
          if [ ! -f eslint-results.sarif ]; then
            cat > eslint-results.sarif << 'EOF'
            {
              "version": "2.1.0",
              "$schema": "https://raw.githubusercontent.com/oasis-tcs/sarif-spec/master/Schemata/sarif-schema-2.1.0.json",
              "runs": [
                {
                  "tool": {
                    "driver": {
                      "name": "ESLint",
                      "informationUri": "https://eslint.org",
                      "rules": []
                    }
                  },
                  "results": []
                }
              ]
            }
          EOF
          fi

      - name: Upload analysis results to GitHub
        uses: github/codeql-action/upload-sarif@1b1aada830fa9cce8900d4145c2a4e3c19f8c0e1
        if: always()
        with:
          sarif_file: eslint-results.sarif
          category: eslint
          wait-for-processing: true

      - name: ESLint Summary
        if: always()
        run: |
          echo "## 🔍 ESLint Security Scan" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "${{ steps.eslint-run.outcome }}" == "success" ]; then
            echo "✅ **ESLint:** No issues found" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ **ESLint:** Issues detected" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Check the **Security** tab for detailed findings." >> $GITHUB_STEP_SUMMARY
          fi
