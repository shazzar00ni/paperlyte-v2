/** Exact waitlist subscriber count used in calculations. */
export const WAITLIST_COUNT_NUMERIC = 500
/** Human-readable waitlist count string displayed in the UI. */
export const WAITLIST_COUNT = `${WAITLIST_COUNT_NUMERIC}+`
/** Target launch quarter displayed in the Email Capture section. */
export const LAUNCH_QUARTER = 'Q2 2026'
